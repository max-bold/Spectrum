1. Application:

    1. При запуске приложение выдает:

    ```
    RuntimeWarning: 'spectrum_app.application' found in sys.modules after import of package 'spectrum_app', but prior to execution of 'spectrum_app.application'; this may result in unpredictable behaviour
    warn(RuntimeWarning(msg))
    ```

    > **Комментарий:** Причина найдена: конфигурация `BM Spectrum (dev)` запускает `spectrum_app.application` как модуль, а `spectrum_app/__init__.py` до этого сам импортирует `application`. В результате один файл загружается дважды. Надо убрать eager-import из `__init__.py` либо запускать пакет через отдельный `spectrum_app/__main__.py`.

    >> Согласен - поправь

    >> Need testing

    2. В настройках при выборе звуковой карты надо ставить block size по умолчанию равным 0.1*fs

    > **Комментарий:** Сейчас при выборе устройства меняется маршрутизация, но `input_block_size`/`output_block_size` не пересчитываются. При явной смене устройства надо выставлять соответствующий размер в `round(device.sample_rate * 0.1)` и сразу обновлять GUI и файл настроек. Простое обновление списка устройств не должно затирать значение пользователя.

    >> Согласен - поправь

    >> Need testing

    3. В app state pane при перовой отрисовке линии должна ставится галочка is_visible для текущего measurement

    > **Комментарий:** Модуль добавляет новый `graph.id` в `visible_graph_ids`, но уже созданный checkbox об этом не узнаёт. Нужна синхронизация `AppStatePane` после обработки `graph_data_changed`; модуль при этом по-прежнему должен знать только про данные и флаг, а не про конкретный GUI-контрол.

    >> Согласен - поправь

    >> Need testing

    4. Если после измерения пользователь пытается изменить module_combo, то нужно запросить у него подверждения и сообщить, что прдыдущие данные измерения будут потеряны

    > **Комментарий:** Сейчас `_set_module()` сразу очищает `module_state` и `graphs`, поэтому потеря данных действительно необратима. Перед этим нужен модальный confirmation; при отмене следует вернуть combo к текущему модулю. Спрашивать достаточно, если у measurement уже есть результаты/сырая запись, пустое новое measurement можно переключать сразу.

    >> Согласен - добавь диалог с подверждением

    >> Need testing

    5. **NEW** При принудительной остановке измерения (Spectrum) приложение падает! Причем без каких либо ошибок!

2. Spectrum:

    1. При обновлении графика постоянно меняется цвет линии - это бесит. Нужно зафиксировать цвет при обновлении

    > **Комментарий:** Причина в `Plot._redraw()`: при каждом обновлении он удаляет series и создаёт её заново, а DPG выдаёт очередной автоматический цвет. Правильное решение — хранить series по `graph.id`, назначать ей стабильную тему/цвет и при обычном обновлении менять данные через `set_value()`. Это заодно устранит часть мерцания RTA.

    >> Да. Давай свяжем конкретную линию с `graph.id` и вместо того что-бы её пересоздавать, будем изменять в ней данные.

    >> Need testing

    2. Длина сигнала не соответвует заданной в right pane (по нижнему графику): 
        - При настройке 20.0 s длина свипа на около 14 
        - При настройке 10.0 s - около 6

    > **Комментарий:** В `test1.bms` сам генератор Spectrum имеет правильные 20 секунд, но записанный chirp достигает 20 кГц примерно к 12.5 секунде и затем остаётся только шум. То же наблюдается в Phase и THD+N. Индексы в Python-циклах записи корректны, однако данные очень похожи на пропуск кадров между блоками (идея с курсором): такой пропуск ускорит chirp, но не изменит частоты стационарного multitone. Нужен отдельный диагностический прогон с логированием номера и размера каждого read/write-блока, wall-clock и `stream.time`, плюс тестовый сигнал с маркерами на границах блоков. До этой проверки менять длительности генераторов нельзя.

    >> Если RAW генераторы в порядке, то дело не в генераторе. Надо разбираться с цепочкой воспроизведение/запись.

    3. Странное поведение сглаживания - при сглаживании 0.3 oct линия оченбь дерганная. При этом входы и выходы звуковой карты соединиены напрямую. При этом АЧХ звуковой карты должно быть близко к линейной - не понятно откуда берется такой шум на FFT. Проверить матиматику расчета FFT и сглаживания по старой версии. Особенно - ширину и форму окна.

    > **Комментарий:** Перенесённые `log_window()` и `log_smooth()` математически совпадают со старым `log_filter2()`, включая ширину и Gaussian. Значит, явной ошибки переноса здесь нет. В проекте отношение спектров A/B действительно гуляет примерно на 12 dB, а запись chirp уже повреждена по времени. Сначала надо устранить пропуски I/O, затем на той же записи сравнить periodogram, Welch и комплексную передаточную функцию/cross-spectrum. Только после этого решать, требуется ли менять FFT-окно или способ расчёта reference.

    >> Да, возможно все дело в битой записи. Давай сначала разберемся с 2.2.

3. Phase:

    1. Нижний график с АЧХ должен быть в db, где 1 = 0 db. Масштаб надо зафиксировать -48 db -- 0 db.

    > **Комментарий:** Сейчас нижний plot получает `result_magnitude_db`, то есть модуль передаёт туда АЧХ A/B, а не уровни входных каналов. Его надо перевести на два нормированных спектра A и B в dBFS (`1.0 = 0 dB`) и явно установить Y limits `-48..0 dB`.

    >> Согласен

    >> Реализовано с учетом п. 3.3: вместо АЧХ нижний график теперь показывает level vs time для A/B.

    >> Need testing

    2. При повторном измерении пропадает линия на нижнем графике

    > **Комментарий:** В начале измерения series очищается, а её Y-ось не имеет фиксированных limits и не фитится после прихода нового результата. Вероятнее всего новая линия существует, но оказывается вне сохранённого диапазона оси. Фиксированный диапазон `-48..0 dB` из предыдущего пункта и обновление двух постоянных series должны устранить это; затем надо проверить повторным реальным прогоном.

    >> Ок. Тогда сначала поправь 3.1. А потом перепроверим этот пункт

    >> Need testing

    3. Нижний график должен отображать спектр для обоих каналов. Наклон графика 10 db/dec надо скомпенсировать.

    > **Комментарий:** Для этого `PhaseResult` должен дополнительно возвращать спектры A и B, а view — иметь две series. Наклон возникает из спектральной плотности logarithmic chirp; для визуального level-графика его можно компенсировать добавлением `10*log10(f/f_ref)` с последующей общей нормировкой к dBFS. Сам основной результат Phase A/B менять не надо.

    >> Вот тут есть небольшая хитрость. Нам нужно видеть именно уорвни входящих сигналов, что-бы вовремя отловит clipping. Давай тогда все таки во всех модулях показывать график level vs time а не спектр. На самом деле он значительно информативнее.

    >> Need testing

    4. Проверить автоустановку "delay fit range" при изменении "freq range". Сейчас она происходит только при при обновлении band[0]. При изменении band[1] ни чего не происходит.

    > **Комментарий:** Причина в текущей логике: `_constrain_delay_fit()` только зажимает старый диапазон внутри нового. Поэтому повышение верхней границы `band[1]` ничего не меняет — старое значение всё ещё допустимо; нижняя граница меняется лишь когда становится недопустимой. Нужна именно политика автоследования: при изменении Frequency range пересчитывать обе границы Delay fit range (либо двигать только ту границу, которая до изменения следовала за краем диапазона).

    >> Да, все правильно. По умолчанию принимаем `fit_range = (band[0]*2, band[1]/2)`. При изменении `band[0]` пересчитываем `fit_range[0]`, при изменении `band[1]` пересчитываем `fit_range[1]`. Я вообще не очень уверен, что эта настройка будет нужна реально. Теоретически она должна позволить исключить часть спектра, в которой есть существенные искажения - отражения, гармоники и т.д. Но не совсем понятно, как пользователь должен их определить.

    5. **NEW** Давай пока вообще зафиксируем `fit_range = band` в коде и уберем это поле из настроек. Потом при тестах возможно вернем, но я не уверен.

    >> Need testing

    6. **NEW** Давай Calculated delay сразу писать в метрах в input_float и пускай уже пользователь сам меняет ее. `self.DELAY_TEXT` оставь (но только в метрах) для того что-бы было понятно к чему возвращаться.

    >> Need testing

4. THD+N:

    1. Длина свипа (по графику) не соответсвует заданной в настройках

    > **Комментарий:** Это тот же общий I/O-дефект, что и в Spectrum: генератор в проекте содержит ровно 30 секунд, а полезный sweep в записи заканчивается примерно на 19-й секунде. Исправлять надо общий путь read/write и продвижение блоков, а не THD-модуль отдельно.

    >> Согласен

    2. Результат измерения возвращает бредове значения - проверить математику. На тестах все было хорошо

    > **Комментарий:** Анализ THD привязывает основную частоту к времени sweep. Если записанный sweep ускорен/имеет пропуски, fundamental попадает за рассчитанную mask и считается шумом/искажениями — это хорошо объясняет значения до десятков процентов при исправных unit-тестах на идеальных данных. Сначала исправляем I/O, затем добавляем интеграционный тест с блоковой записью и проверяем математику уже на корректном реальном файле.

    >> Да. Очень похоже на проблемы с IO. Пока ни чего не трогаем

    3. Проверить сглаживание. Сейчас при сглаживании 0.333 oct линия очень дерганная. Раньше так небыло.

    > **Комментарий:** THD использует собственный `_smooth_log_ratio()`, а не общий `audioanalysis.log_smooth()`: прямоугольное окно и RMS по ratio. Это надо отдельно сравнить со старой реализацией и, вероятно, перевести на общий оконный механизм. Но текущая кривая прежде всего построена из неверно отслеженного sweep, поэтому оценивать сглаживание до исправления I/O бессмысленно.

    >> Согласен

5. RTA:

    1. Сейчас bars плотно стыкуются - лучше дать 2-3 px расстояния между ними. Визуально это будет выглядеть аккуратнее

    > **Комментарий:** Сейчас соседние границы вычисляются как один и тот же geometric midpoint, поэтому прямоугольники соприкасаются точно. Зазор лучше делать после преобразования координат в pixels — сдвигать каждую сторону на 1–1.5 px. Частотные границы самих полос при этом останутся математически правильными.

    >> Согласен

    >> Need testing

    2. При обновлении bars морагют - это очень раздражает визуально

    > **Комментарий:** Есть две причины: общий Plot удаляет и создаёт custom series при каждом snapshot, а callback bars дополнительно удаляет и создаёт все rectangles. Нужен постоянный объект series и обновление существующих draw-items/координат без промежуточного пустого кадра. Это стоит решать вместе с фиксацией цветов Spectrum в инкрементальном renderer Plot.

    >> Согласен. Обновление series вместо их пересоздания скорее всего решит проблему. Пока отложим.

    3. Нужно скомпенсировать наклон графика

    > **Комментарий:** Сейчас RTA усредняет PSD розового шума и сразу переводит её в dB, поэтому естественный наклон `-10 dB/dec` остаётся на графике. Для равных логарифмических полос надо учитывать их ширину: эквивалентно умножить density на частоту (с выбранной постоянной нормировки) перед переводом в dB. Это reusable math и должно жить в `audioanalysis/rta.py`.

    >> Учитывая, что ширина полос на лог шкале равна, то я думаю будет достаточно просто умножить значение на `sqrt(f)`. 

    >> `periodogram()` возвращает power density, поэтому в коде используется эквивалентная операция `density * f`; `sqrt(f)` применяется к amplitude spectrum.

    >> Need testing

    4. bars должны рисоваться от нижней границы графика вверх. Сейчас они рисуются от нуля вниз ()

    > **Комментарий:** Причина точная: `_add_bar_series()` жёстко создаёт `baseline = 0`. В custom callback нижнюю координату надо брать из текущей видимой нижней границы Y-axis/plot rectangle, чтобы bars всегда росли от низа и корректно реагировали на zoom/autoscale.

    >> Согласен. Сделай.

    >> Need testing

    5. Надо дать пользователю возможность изменения количества точек (столбиков) и ширины окна сглаживаня во время измерения.

    > **Комментарий:** Сейчас на время измерения отключается весь `RTAView`, а `_runtime_analysis_config` фиксируется при старте. Надо оставить активной секцию Smoothing и атомарно публиковать анализатору новую immutable-конфигурацию для следующего snapshot. Генератор и acquisition перезапускать не требуется; переключение bars/line при изменении points также должно происходить без пересоздания всего Plot.

    >> Согласен

    >> Need testing

    6. Еще нам нужно будет разобраться с автоподстройкой ширины сглаживающего окна. Напомни мне потом. А лучше сразу добавь в TODO для версии 0.3.1.

    >> Added to TODO

6. Impedance:

    1. На первом этапе калибровки вылетает ошибка: "Impedance error: Channel calibration failed: recording is not the generated multi tone signal". Дальше пройти не смог.

    >> **Комментарий:** Проверка падает, когда энергия записи не сосредоточена около ожидаемых частот multitone. В сохранённой calibration recording часть тонов совпадает, но особенно низкочастотная часть содержит лишнюю/смещённую энергию. Это совместимо с обнаруженными пропусками между I/O-блоками: непрерывные частоты сохраняются, но скачки дают широкополосную энергию. Сначала нужен тот же диагностический I/O-тест с block markers; после него отдельно вывести diagnostics validator (энергия tone/off-tone и найденные peaks), а не только итоговую ошибку.

    >> Да. Это скорее всего таже проблема с IO. Пока не трогаем. Нужно дополнительное тестирование.

7. Plot:

    1. Легенда должна отображаться всегда. Сейчас она появляется только, когда выбрано 3 и более измерений.

    > **Комментарий:** В коде legend создаётся без условного порога, то есть это не запланированное поведение. Вероятные причины — состояние/пересоздание series либо размещение DPG legend. При переделке renderer надо задать legend явные `show=True` и location, затем проверить на 1, 2 и 3 line/custom series. По одному коду точную причину этого пункта пока установить нельзя.

    >> Ок. Будем разбираться отдельно.

    2. Нужен автомасштаб основного графика при первой отрисовке новой линии (при добавлении измерения, изменении списка измерений). 

    > **Комментарий:** Сейчас `_redraw()` устанавливает только X limits и ни разу не вызывает fit для Y. Plot может сам сохранить диапазон пустого графика `0..1`. Нужно определять изменение topology (новый/удалённый/скрытый graph или набор осей) и только тогда вызывать `fit_axis_data()` для видимых Y-осей. При обычном online-обновлении данных автоfit делать не следует, иначе пользователь не сможет приблизить график.

    >> Согласен

    >> Need testing

    3. При отрисовке более чем одной оси У - одна из них должна располагаться справа. При этом шкала в db всегда рисуется слева. 

    > **Комментарий:** `AxisSpec.LEVEL` уже первый в приоритете, но дополнительные DPG axes создаются без явного размещения, поэтому в текущей версии все оказались слева. Надо всегда назначать Level основной левой `mvYAxis`, а для второй оси явно включать opposite/right side. Раскладку третьей оси зададим явно после проверки возможностей текущей DPG, чтобы подписи не накладывались.

    >> Да, у тебя есть `AXIS_ORDER` в plot.py. В принципе я с ним согласен. При добавлении новой оси мы смотрим на него. Ось с меньшим номером в списке рисуется слева, все остальные - справа.

    >> Need testing
